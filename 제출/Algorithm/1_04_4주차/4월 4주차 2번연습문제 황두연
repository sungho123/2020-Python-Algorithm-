list = []     # 학생점수 리스트

for i in range(0,5)
   score = int(input())
    
    if score < 40:     # 40점 미만인 학생은 40점으로
         score = 40
            
    list.append(score)
    
print(int(sum(list) / 5 ))     # 평균 점수
